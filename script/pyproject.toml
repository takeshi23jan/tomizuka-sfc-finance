[project]
name = "hamamatsu-doc-downloader"
version = "0.1.0"
description = "Download documents linked from the Hamamatsu City page"
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.11.10",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
